// Single source of truth for site-wide constants shared between the page,
// the metadata in app/layout.js and the JSON-LD payload.

export const SITE_URL = "https://cualli.bio";

// Public contact address, as specified by the design. NOTE: the previous
// revision of the site used olinares@cualli.bio — confirm this alias is
// receiving mail before deploying.
export const CONTACT_EMAIL = "hello@cualli.bio";
